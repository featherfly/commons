
package cn.featherfly.common.db.dialect;

/**
 * Join Types .
 *
 * @author zhongj
 */
public enum Join {

    /** The inner join. */
    INNER_JOIN,
    /** The left join. */
    LEFT_JOIN,
    /** The right join. */
    RIGHT_JOIN,
    /** The full join. */
    FULL_JOIN;
}
