
package cn.featherfly.common.db.dialect;

/**
 * <p>
 * Keywords
 * </p>
 * .
 *
 * @author zhongj
 */
public enum Keywords {

    /** The table. */
    TABLE,

    /** The column. */
    COLUMN,

    /** The view. */
    VIEW,

    /** The index. */
    INDEX,

    /** The select. */
    SELECT,

    /** The from. */
    FROM,

    /** The where. */
    WHERE,

    /** The insert. */
    INSERT,

    /** The into. */
    INTO,

    /** The values. */
    VALUES,

    /** The update. */
    UPDATE,

    /** The set. */
    SET,

    /** The delete. */
    DELETE,

    /** The join. */
    JOIN,

    /** The on. */
    ON,

    /** The inner. */
    INNER,

    /** The left. */
    LEFT,

    /** The right. */
    RIGHT,

    /** The outer. */
    OUTER,

    /** The full. */
    FULL,

    /** The corss. */
    CORSS,

    /** The create. */
    CREATE,

    /** The drop. */
    DROP,

    /** The after. */
    AFTER,

    /** The add. */
    ADD,

    /** The modify. */
    MODIFY,

    /** The truncate. */
    TRUNCATE,

    /** The order. */
    ORDER,

    /** The by. */
    BY,

    /** The desc. */
    DESC,

    /** The asc. */
    ASC,

    /** The like. */
    LIKE,

    /** The in. */
    IN,

    /** The is. */
    IS,

    /** The null. */
    NULL,

    /** The not. */
    NOT,

    /** The between. */
    BETWEEN,

    /** The union. */
    UNION,

    /** The intersect. */
    INTERSECT,

    /** The except. */
    EXCEPT,

    /** The and. */
    AND,

    /** The or. */
    OR,

    /** The all. */
    ALL,

    /** The as. */
    AS,

    /** The count. */
    COUNT,

    /** The sum. */
    SUM,

    /** The max. */
    MAX,

    /** The min. */
    MIN,

    /** The avg. */
    AVG,

    /** The distinct. */
    DISTINCT,

    /** The database. */
    DATABASE,

    /** The alter. */
    ALTER,

    /** The if. */
    IF,

    /** The exists. */
    EXISTS,

    /** The comment. */
    COMMENT,

    /** The primary. */
    PRIMARY,

    /** The key. */
    KEY,

    /** The constraint. */
    CONSTRAINT,

    /** The default. */
    DEFAULT;
}
